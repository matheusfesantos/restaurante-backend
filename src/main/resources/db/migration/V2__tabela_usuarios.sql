CREATE TABLE usuarios (
                          id INT PRIMARY KEY,
                          nome VARCHAR(80),
                          username VARCHAR (25),
                          senha TEXT
);
